name: "Disabled - Jekyll chosen"

on:
  push:
    branches: [ main ]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Jekyll will be built by GitHub Pages. This workflow is a placeholder."
