---
layout: base.njk
title: Blog
---

<section class="hero" style="padding-bottom: 12px;">
  <h1>Blog</h1>
  <p class="muted">Articles, tutorials, and updates about Interlaced Pixel projects</p>
</section>

<section class="projects" style="max-width:860px;margin:0 auto; padding-bottom:80px;">
  {% for post in collections.posts %}
    <article class="card" style="margin-bottom:18px;">
      <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
      <p class="muted">{{ post.date.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) }}</p>
      <p>{{ post.data.excerpt || post.templateContent | striptags | truncate(160) }}</p>
      <a href="{{ post.url }}" class="card-link">Read <span class="material-icons-outlined">arrow_forward</span></a>
    </article>
  {% else %}
    <p>No posts yet.</p>
  {% endfor %}
</section>