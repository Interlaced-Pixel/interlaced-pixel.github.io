Eleventy blog (quick start)

Development

1. Install dependencies

   npm ci

2. Start dev server

   npm start

Creating posts

- Create a Markdown file in `src/posts/` with front matter fields: `title`, `date`, `description`. The admin UI is available at `/admin/` once Decap is configured.
